# Develop override settings

# Use relative URLs for local preview so assets work on any local port.
url: ""

analytics:
  provider: false

comments:
  disqus:
    shortname            : "mmistakes-dev"

sass:
  style: expanded

# Avoid GitHub API calls during local preview (prevents metadata hangs).
github: false
